@AGENTS.md

# BGN 잠실 — 3D·스크롤 인터랙션 랜딩

## 목표

- 시안(`docs/design/2026_BGN 잠실.pdf`)을 기준으로 단일 스크롤 랜딩을 구현한다.
- "보이는 것"뿐 아니라 **프레임 타이밍·관성·스크롤 연동**까지 매끄러운 체감 품질을 우선한다.

## 시안 읽는 법

PDF 7페이지의 정체는 이렇다. 긴 웹페이지 한 장이 아니라 **스크롤 상태별 프레임 스택**이다.

| 페이지 | 크기       | 정체                                           |
| ------ | ---------- | ---------------------------------------------- |
| 1      | 1920×21975 | **PC 메인** — 1920×920 프레임 24장             |
| 2      | 1920×920   | PC 헤더 + 히어로 단독 컷                       |
| 3      | 1160×3840  | 진료센터 카드 **확장(hover)** 상태 6종         |
| 4      | 375×17336  | **모바일 메인** — 375×812 프레임 22장          |
| 5      | 524×520    | 진료센터 **축소** 상태 6종 (각 87px, 세로쓰기) |
| 6      | 1625×520   | 진료센터 기본 상태 (사진 포함, 각 260px)       |
| 7      | 1640×640   | 진료센터 기본 상태 (그라데이션만)              |

3·5·6·7 은 서로 다른 화면이 아니라 **같은 아코디언 컴포넌트의 상태 세트**다. 산술이 맞는다:

```
default : 262 × 6           = 1572 (+gap) ≈ 1640
hover   : 1160 + 87 × 5     = 1595 (+gap) ≈ 1640
```

## 섹션 구성 (시안 스크롤 순)

| #   | 섹션               | 폴더             | 핵심 과제                                                        |
| --- | ------------------ | ---------------- | ---------------------------------------------------------------- |
| 1   | 히어로             | `hero`           | 파티클 구체 3D + 마퀴 + 스크롤 인디케이터. 장면 2개(구체 / 리본) |
| 2   | 브랜드 슬로건      | `brand-slogan`   | 이리데슨트 리본 3D + 롯데타워. 패럴랙스 3레이어                  |
| 3   | 브랜드 필름        | `brand-film`     | 유리 굴절 로고 + 라이트 트레일. **UI 전부 숨김**, 유일한 웜톤    |
| 4   | 의료진             | `medical-team`   | 센터 포커스 캐러셀 8인                                           |
| 5   | AI 정밀검사 시스템 | `ai-system`      | 4스텝 카드 + 데이터 비주얼(카운트업·게이지·레이더) + 마퀴        |
| 6   | AI 상담 신청       | `ai-consult`     | 폼 + 유리 `B` 3D. **유일한 중간톤 블록**                         |
| 7   | AI 브랜드 스토리   | `ai-story`       | 탭 3개 + 대형 비디오 카드                                        |
| 8   | 진료 센터          | `centers`        | 가로 아코디언 6종. hover 의존 → 터치는 스와이퍼                  |
| 9   | 히스토리           | `history`        | **pinned** 5스텝 + 카드 스택 회전                                |
| 10  | Web blog           | `web-blog`       | **pinned 가로 스크롤**. 다크 타일 ↔ 화이트 카드 교차             |
| 11  | 이벤트             | `event`          | 스와이퍼 + 프로그레스 바(도트 아님)                              |
| 12  | 클로징 스피어      | `closing-sphere` | 파티클 3D. 텍스트 없음, 히어로 오브젝트 재사용                   |
| 13  | 컨택트 + 푸터      | `footer-contact` | 지점 탭(잠실/부산) + 진료시간 + 사업자정보                       |

전역 UI: 플로팅 pill GNB / 메가메뉴 / 언어 드롭다운(4개국어) / PC 팬아웃 퀵바 / 모바일 하단 고정바.

## 소스 구조

- **`src/app/`** — 라우터에만 연결된 `page.tsx` / `layout.tsx`. 로직 0줄.
- **`src/components/`** — 여러 페이지가 쓰는 순수 UI. **i18n messages 를 import 하지 않는다.**
- **`src/features/<영역>/`** — 섹션·도메인. `messages` 를 props 로 받는다.
  해당 영역에만 쓰는 훅·상수는 **같은 폴더에** 둔다.
- **`src/r3f/`** — 3D 공통 인프라. 새 3D 씬은 `CanvasShell` 로 시작한다.
- **`src/shared/config/`** — breakpoints, i18n, nav. 단일 소스 상수.

## 기술 스택

- **Next.js 16** App Router, React 19 — Canvas·R3F 는 `dynamic(..., { ssr: false })`.
- **GSAP + @gsap/react** — 스크롤 구동 전담. 플러그인 등록은 `@/shared/lib/gsap` 한 곳에서만.
- **Lenis** — 스무스 스크롤. `gsap.ticker` 에 통합돼 있다 (`smooth-scroll-provider.tsx`).
- **Three.js + R3F + drei** — 3D. `frameloop` 은 가시성으로 제어.
- **Motion** — 인덱스가 바뀔 때의 크로스페이드 보조. **스크롤 구동에는 쓰지 않는다.**

## 역할 경계 (이걸 흐리면 반드시 문제가 난다)

| 도구               | 담당                                       |
| ------------------ | ------------------------------------------ |
| GSAP ScrollTrigger | 스크롤 → 진행도/인덱스 변환, pin           |
| ref                | 매 프레임 값을 3D 로 전달 (**state 금지**) |
| Motion             | 인덱스 변경 시 크로스페이드                |
| R3F                | 렌더                                       |

넷이 같은 요소의 같은 속성을 동시에 건드리지 않는다.

## 새 섹션 만들기

- 스크롤 연동 3D 섹션 → `sections/hero/` 또는 `sections/history/` 복사
- pin + 진행도 → `usePinnedProgress()`
- 단순 등장 모션 → `useSectionReveal()` 하나면 끝
- 마퀴 → `<Marquee />` (히어로·AI 섹션이 같은 컴포넌트를 쓴다. 새로 만들지 말 것)

## 디자인 토큰

값은 **Figma 변수에서 직접 가져온 실값**이다. 전부 `src/app/globals.css` 에 있고,
Figma 변수명 ↔ CSS 변수 대조표는 `docs/plan/06-css-variables-proposal.md` 에 있다.

```
--primary-500    #0072ec   CTA · 활성 탭 · 링크        (primary/primary/500)
--primary-900    #0a2048   헤드라인 최농도             (primary/primary/900)
--primary-100    #e2effe   형광 마커 배경              (primary/primary/100)
--primary-sub-500 #8d79ff  그라데이션 보라 끝          (primary/sub/500)
--gray-800       #242424   GNB 네비 텍스트
--whitebg-90     #ffffffe5 GNB 배경 (+ backdrop-blur 25px)
```

**폰트 (Figma 확정)**: 국문·본문 `Pretendard` / 영문 포인트 `Belleza`.
Belleza 는 Regular 400 단일 웨이트다 — 합성 볼드 금지.

**타이포**: Figma 텍스트 스타일 1:1 (`--t-*`), 반응형은 `--fs-*` clamp.
clamp 의 min 은 임의 축소가 아니라 `mo/*` 실값이다.
letterSpacing 은 % 단위 → `-3` = `-0.03em` (`--tracking-default`).

**레이아웃 (Figma 실측)**

```
콘텐츠 1760 / 좌우 80        모바일 335 / 거터 20
GNB    1840 × 80, top 24, inset 40, radius 16, blur 25
센터카드 기본 240 / 확장 1120 / 축소 64 / gap 32 / radius 24
  → 240×6 + 32×5 = 1600  ·  1120 + 64×5 + 32×5 = 1600
```

섹션 헤더는 예외 없이 3단이다: `.eyebrow`(Belleza 32 블루) →
`.section-title`(Pretendard Bold 42) → `.section-desc`(20). 개별 섹션에서
다시 정의하지 말 것.

## Figma 참조

파일 `2xzudppVSWEHbxVdzofgn3` / 루트 `2:405`.
노드 ID 매핑은 `docs/plan/00-pages-overview.md` 참고.

⚠️ 이 파일은 **WACUS 팀으로 복제한 사본**이다. Figma MCP 가 파일 단위 공유가
아니라 플랜 멤버십을 요구해서 원본에 접근할 수 없었다. 디자이너가 원본을
수정해도 동기화되지 않으므로, 시안이 갱신되면 재복제해야 한다.

## 인터랙션 스펙 — 반드시 먼저 읽을 것

`docs/plan/07-interactions.md` 에 **디자이너 Dev Mode 주석 원문**이 있다.
PDF 에는 없고 Figma 주석에만 있던 내용이라, 이걸 안 보고 만들면 방향이 어긋난다.

레퍼런스 사이트 6개가 지정돼 있다 (주석: "레퍼사이트 대로 구현 필요"):

| 사이트        | 대상                 |
| ------------- | -------------------- |
| auremin.com   | **히어로 구체 모션** |
| the1seoul.com | 퀵메뉴 펼침          |
| handhold.io   | 메인_02 배경 텍스처  |
| ambiq.ai      | AI 검사 카드         |
| cynx.io       | 진료센터 캐러셀      |
| skazy.ai      | 히스토리 이미지 카드 |

### 히어로 구체 (핵심)

```
로딩 화면 없이 진입 시 지구형태가 약간돌면서 등장 후
마우스 포인터에 맞춰 빛 요소가 움직이도록
```

- **로딩 화면 금지.** 스플래시/프로그레스 넣지 말 것.
- Figma 안의 구체는 사전 렌더 PNG 2장 블렌드다. 3D 소스가 없다.
  → 이미지 2장으로는 위 모션을 못 맞춘다. **PNG 구체 + WebGL 빛 레이어**가
  현실적인 1순위. 디자이너에게 3D 원본이 있는지 먼저 확인할 것.

### ⚠️ 영상인데 3D 로 만들고 있던 것

주석 확인 결과 **3D 가 아니라 영상**인 구간이 3곳이다:

- `brand-film` — `2:994` "BGN잠실 메인페이지 영상_2차본", 1920×1080
- `ai-consult` 우측 762×762 — "영상 삽입 예정" (빈 프레임)
- `web-blog` 배경 — 영상 + `backdrop-blur 75px` + `rgba(0,114,236,0.2)`

→ 3D 씬은 5개가 아니라 실질 **1~2개**(히어로 구체 + 클로징 재사용)다.

## 함정 목록

- `position: sticky` 대신 GSAP `pin` 을 쓴다. sticky 는 조상 `overflow:hidden` 에 죽는다.
  이 사이트는 pin 섹션이 3개(브랜드필름/히스토리/웹블로그)라 특히 치명적이다.
- reduced-motion 을 early-return 으로 처리하지 말 것. `settleReducedMotion()` 으로
  최종 상태 + `clearProps` 까지 해야 콘텐츠가 사라지지 않는다.
- lerp 는 `1 - Math.exp(-delta * k)` 로. `(b-a)*0.1` 은 120Hz 에서 두 배 빨라진다.
- 내부 스크롤 영역(메가메뉴, 가로 트랙, 탭 바)에는 **반드시 `data-lenis-prevent`**.
  Lenis 가 wheel/touch 를 가로채서 안 움직인다.
- hover 의존 컴포넌트는 폭이 아니라 `MQ.hoverable`(pointer: fine)로 가드할 것.
  1100px 터치 노트북에서 아코디언이 걸려 조작 불가가 된다.
- `html { font-size: clamp(10px, 0.8333vw, 16px) }` — 데스크톱에서 rem 이 뷰포트에
  비례해 축소된다. 1024px 이하만 16px 고정. **rem = 16px 고정이라 가정하면 전부 어긋난다.**
- GNB 는 불투명 흰색이 아니다. `whitebg-90` + `backdrop-blur(25px)` + 1px 흰 보더.
  solid 로 바꾸면 시안과 완전히 달라진다.
- 네비 라벨 구분자는 `·`(U+00B7)가 아니라 **`∙`(U+2219)** 다. `nav.ts` 에서 복사할 것.
- 진료센터는 **hover 아코디언이 아니다.** 주석: "해당 영역 클릭시 현재 선택된
  센터 이후의 3가지 영역이 펼쳐짐(모바일에선 제거)". 현재 구현과 동작이 다르다.
- 3D 씬에서 `Math.random()` 금지. React Compiler 가 불순 함수로 잡고, 렌더마다
  분포가 달라져 디자인 리뷰 비교가 안 된다. 시드 PRNG 를 쓸 것(`hero-scene.tsx` 참고).
- GLB 를 쓸 때 URL·숫자 상수는 three 를 import 하지 않는 별도 `*-config.ts` 로 분리.
  안 그러면 `useGLTF.preload()` 가 초기 로드에 딸려온다.
- draco 압축본을 쓰려면 `public/draco/` 디코더 + `useGLTF(url, "/draco/")` 가 필요하다.

## 완료 기준

- 시안 주요 섹션이 스크롤 순서대로 구현되고 시각적으로 대응된다.
- 모바일(375 기준)에서 레이아웃 붕괴 없이 사용 가능하다(3D 는 파라미터 축소 허용).
- `prefers-reduced-motion` 과 키보드 내비게이션이 동작한다.
- `next build` · `tsc --noEmit` · `eslint` · `analyze:chunk` 전부 통과.

## 확인이 필요한 것 (디자이너/기획)

`docs/design-analysis.md` 의 마지막 절 참고. 요약:

1. 히어로 장면 2개가 캐러셀인가, 스크롤 전환인가
   1b. **브랜드 필름 구간(`2:994`)이 3D 가 아니라 영상일 가능성** — 노드명이
   "BGN잠실 메인페이지 영상_2차본 1" 이고 1920×1080 이다. 확인 필요 (작업량이 크게 달라짐)
2. ~~영문 디스플레이 폰트~~ → **Belleza 로 확정** (Figma 변수)
3. 진료센터 명칭 표기 통일 (스마일센터 / 스마일라식센터가 시안에 혼재)
4. 4개국어를 실제로 운영하는가
5. 푸터에 지도를 넣는가 (시안에는 주소 텍스트만)
6. AI 섹션 4개 데이터 비주얼의 3D 여부 (전부 3D 는 프레임 예산 초과)
